# Google OAuth 
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Server ports
SMTP_PORT=2525
HTTP_PORT=3000

# Database
DATABASE_PATH=./data/relay.db

# Encryption key for securing tokens at rest (required)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
